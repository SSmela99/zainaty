/**
 * Tailwind-aligned project breakpoints.
 *
 * - mobile: 0-767px (`< md`)
 * - tablet: 768-1023px (`md` to `< lg`)
 * - desktop: 1024px+ (`lg+`)
 */
export const BREAKPOINTS = {
  tablet: "768px",
  desktop: "1024px",
} as const;
